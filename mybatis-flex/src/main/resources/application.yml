spring:
    application:
        name: mybatis-flex
    profiles:
        active: pg
#        active: sqlserver
server:
    port: 10004
    servlet:
        context-path: /flex
eureka:
    client:
        service-url:
              defaultZone: https://localhost:10000/eureka/